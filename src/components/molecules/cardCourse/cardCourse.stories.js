import { cardCourse } from './cardCourse';

const meta = {
  title: 'molecules/CardCouse',
  component: cardCourse,
  tags: ['autodocs'],
  argTypes: {
    startDate: {
      control: 'text',
    },
  },
};

export const CardCourse = {};

export default meta;
